Import('env')

if env.get('HAVE_CPPUNIT'):

	test_env = env.Clone()

	test_env.Append(
		LIBS = ['microstranparser']
		,LIBPATH = ['..']
		,CPPPATH = ['..']
	)

	tests = Split("""
		testvec3.cpp
		testctrans.cpp
	""")
	
	test_env.Program("runtest",["main.cpp"] + tests)
